<%= form_with model: [ post, comment], url: admin_post_comments_path(post) do |f| %>
  <%= f.text_area :comment, rows:'10', placeholder: "コメントをここに", class: "w-100" %>
  <%= f.submit "送信" %>
<% end %>

