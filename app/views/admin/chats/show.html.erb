<div class="chat-container">
  <div class="chat-topbar">
    <h3><%= @user.full_name %>様とのチャット</h3>
  </div>
  <div class="message">
    <%= render "admin/chats/messages", chats: @chats, user: @user %>
  </div>

  <div class="chat-form">
    <%= form_with model: @chat, url: admin_chats_path, data: {remote: true} do |f| %>
      <div class="errors">
        <%= render "layouts/error_messages", resource: @chat %>
      </div>
      <%= f.text_field :message, placeholder: "メッセージを入力してください", autocomplete: "off" %>
      <%= f.hidden_field :room_id, value: @room.id %>
      <%= f.submit "送信" %>
    <% end %>
  </div>
</div>